[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "agentictools"
version = "1.2.1"
description = "AI Visit website: discovery, public or authorized Chrome batch reading, Markdown archiving, and search for AI agents."
requires-python = ">=3.12"
dependencies = [
  "crawl4ai==0.9.2",
  "mcp>=2.0,<3",
  "pydantic>=2.11,<3",
  "PyYAML>=6.0,<7",
]

[project.optional-dependencies]
test = [
  "pytest>=8.4,<9",
  "pytest-asyncio>=1.1,<2",
]

[project.scripts]
agentictools-mcp = "agentictools.mcp_server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/agentictools"]

[tool.pytest.ini_options]
addopts = "-q"
asyncio_mode = "auto"
testpaths = ["tests"]
