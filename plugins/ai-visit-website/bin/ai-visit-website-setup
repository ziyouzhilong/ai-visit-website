#!/usr/bin/env python3
"""Install the bundled AI Visit website server into isolated plugin data."""

from pathlib import Path
import subprocess
import sys


launcher = Path(__file__).with_name("ai-visit-website-mcp")
raise SystemExit(subprocess.call([sys.executable, str(launcher), "--setup", *sys.argv[1:]]))
